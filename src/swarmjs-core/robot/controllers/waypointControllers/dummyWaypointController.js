/* eslint-disable arrow-body-style */
/* eslint-disable no-unused-vars */
export default function dummyWaypointController(robot, params) {
  return (goal, sensors, actuators) => {
    return goal;
  };
}
